# excel-vba-merge-multiple-files
This VBA macro allows you to select multiple Excel files and merge all worksheets from those files into a single new workbook.  Each worksheet is copied as a separate sheet (no data appending), and duplicate sheet names are handled automatically.
